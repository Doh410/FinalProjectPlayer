<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | PlayerVault</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap" rel="stylesheet" />
</head>
<body>

  <nav>
    <a href="/" class="nav-brand">🏀 PlayerVault</a>
    <div class="nav-links">
      <a href="/" class="<%= page === 'home' ? 'active' : '' %>">Home</a>
      <a href="/search" class="<%= page === 'search' ? 'active' : '' %>">Search Players</a>
      <a href="/lineup" class="<%= page === 'lineup' ? 'active' : '' %>">My Lineup</a>
      <a href="/cut" class="<%= page === 'cut' ? 'active' : '' %>">Cut Player</a>
    </div>
  </nav>
